package com.drapala.shortlinkgenerator.fillDb;

import com.drapala.shortlinkgenerator.sercive.GeneratorService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

/*
@Component
public class Filler implements CommandLineRunner {


    @Autowired
    private GeneratorService service;

    @Override
    public void run(String... args) throws Exception {
        service.process("aaaaaaaa");
        for (int i = 0; i < 10000; i++) {
            service.process("aaaaaaaa" + i);
        }
    }
}
*/
